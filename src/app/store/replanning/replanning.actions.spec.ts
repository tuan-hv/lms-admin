import * as ReplanningActions from './replanning.actions';

describe('Replanning', () => {
  it('should create an instance', () => {
    expect(ReplanningActions.LoadReplannings()).toBeTruthy();
  });
});
